(function () {  "use strict";  const CONFIG = window.__JOURNEY_LEADERBOARD_CONFIG__ || {};  const STORAGE_PREFIX = "new-mother-journey-leaderboard-v1";  const labels = { energy: "体力", calm: "从容", ready: "准备", team: "默契" };  let endingPayload = null;  let modal = null;  function configured() {    return /^https:\/\/[a-z0-9-]+\.supabase\.co$/i.test(CONFIG.supabaseUrl || "") &&      /^(eyJ|sb_publishable_)/.test(CONFIG.supabaseAnonKey || "");  }  function anonymousId() {    const key = STORAGE_PREFIX + ":anonymous-id";    let value = localStorage.getItem(key);    if (!value) {      value = crypto.randomUUID ? crypto.randomUUID() : "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, function (c) {        const r = Math.random() * 16 | 0;        return (c === "x" ? r : (r & 3 | 8)).toString(16);      });      localStorage.setItem(key, value);    }    return value;  }  function cleanNickname(value) {    const cleaned = (value || "").trim().replace(/[<>]/g, "").replace(/[^\p{L}\p{N}_\- ]/gu, "").slice(0, 16);    return cleaned || "匿名旅人";  }  function findStats() {    const values = Array.from(document.querySelectorAll(".final-stats span"));    const stats = { energy: 0, calm: 0, ready: 0, team: 0 };    values.forEach(function (node, index) {
